import 'package:flutter/material.dart';

class Reportaccidentarea extends StatelessWidget {
  const Reportaccidentarea({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}